<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Timesheet Manager</title>
    <meta charset="utf-8" />
    <style>
      h1 {
        text-align: center;
      }

      button {
        margin-right: 30px;
      }

      .buttons {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100px;
      }

      .flash-message {
        color: #33cc99;
        background: #CBF2E5;
      }

    </style>
  </head>
  <body>
    <header>
      <h1>Welcome to the Timesheet Manager</h1>
      <% if session[:message] %>
      <div class="flash-message">
        <p><%= session.delete(:message) %></p>
      </div>
    <% end %>
    </header>
    <main>
      <div class="buttons">
        <form method="get" action="/input_time">
          <button name="input_time" type="submit">Input Time</button>
        </form>
        <form method="get" action="create_invoice">
          <button name="create_invoice" type="submit">Create Invoice</button>
        </form>
        <form method="get" action="create_report">
          <button name="create_report" type="submit">Create Monthly Report</button>
        </form>
      </div>
    </main>
  </body>
</html>
